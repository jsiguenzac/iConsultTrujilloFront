import styled from 'styled-components';
import { HeaderButton } from '../../styled';

export const HeaderNotificationWrapper = styled.div``;

export const HeaderNotificationButton = styled(HeaderButton)``;
